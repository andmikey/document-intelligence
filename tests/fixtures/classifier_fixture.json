{
  "_comment": "Offline fixture for the classifier node. Used by LocalFixtureBackend when LOCAL_DEV_MODE=true. The 'default' entry is returned for any image not matched by sha256 prefix.",
  "default": {
    "category": "chat_screenshot",
    "category_confidence": 0.95
  }
}
